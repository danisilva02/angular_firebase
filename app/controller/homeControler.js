angular.module('app').controller('homeControler', homeControler);

homeControler.$inject = ['$scope', '$rootScope', '$location', 'APP_SETTINGS'];

function homeControler($scope,$rootScope,$location,APP_SETTINGS){
    
    
}